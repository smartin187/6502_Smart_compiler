# Screen tool

A simple library for screen manipulation of SmartyKit.

## Features & import

Import the library with:

```Smart
import "screen_tool/screen_tool.sma";
```

### Clear screen

The function `screen_clean` clears the screen. It print 24 carrings returns.

For use:

```Smart
screen_clean:;
```
